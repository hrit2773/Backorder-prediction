#End to end implementation of Backorder Prediction machine learning project
#requirements.txt file includes all the packages which are to be installed during the project development-run the command "pip install -r requirements.txt".
#setup.py file is helpful inorder to build and distribute various python packages. It can install the whole application as a package itself.
#logger.py 
